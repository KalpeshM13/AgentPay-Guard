"""AI services package — optional LLM integrations for explainability.

Providers are only active when their API key is configured in the
environment.  Missing keys → graceful degradation with fallback text.
"""
