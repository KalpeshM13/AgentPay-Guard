# =============================================================================
# AgentPay Guard — Production Dependencies
# =============================================================================
# Install:  pip install -r requirements.txt
#
# Keep this file minimal — Render free tier has limited disk space.
# Only list packages that are actually imported in the app/ tree.
# =============================================================================

# -- Web framework -------------------------------------------------------------
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0

# -- Configuration -------------------------------------------------------------
pydantic-settings>=2.6.0,<3.0
email-validator>=2.2.0,<3.0      # EmailStr in Pydantic schemas

# -- Database ------------------------------------------------------------------
sqlalchemy[asyncio]>=2.0.30,<3.0
aiosqlite>=0.20.0,<1.0

# -- Authentication ------------------------------------------------------------
python-jose[cryptography]>=3.3.0,<4.0   # JWT
bcrypt>=4.2.0,<5.0                       # password hashing

# -- Utilities -----------------------------------------------------------------
python-multipart>=0.0.9      # OAuth2 form-data parsing (POST /auth/login)

# -- Testing (optional — omit from Render free tier) ---------------------------
# pytest>=8.3.0,<9.0
# pytest-asyncio>=0.24.0,<1.0
# httpx>=0.27.0,<1.0
